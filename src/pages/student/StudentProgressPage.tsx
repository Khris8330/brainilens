import { Flame, Trophy } from 'lucide-react'
import { Card, CardHeader, CardTitle, CardContent, ProgressBar } from '@/components/ui'
import { subjects, getMockStudents, initialAssignments } from '@/data/mockData'
import { useAuth } from '@/contexts/AuthContext'
export function StudentProgressPage() { const { user } = useAuth(); const student = getMockStudents().find((item) => item.id === user?.id) ?? { progress: 0, streakDays: 0 }; const completed = initialAssignments.filter((item) => item.status === 'completed').length; return <div className="space-y-6"><div><h1 className="text-2xl font-semibold text-text">My Progress</h1><p className="mt-1 text-sm text-text-muted">See how your learning is growing.</p></div><div className="grid gap-4 sm:grid-cols-3"><Card><CardContent className="p-5"><p className="text-xs text-text-muted">Overall progress</p><p className="mt-1 text-3xl font-semibold text-text">{student.progress}%</p></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><Flame className="size-6 text-accent-hover" /><div><p className="text-2xl font-semibold text-text">{student.streakDays} days</p><p className="text-xs text-text-muted">Current streak</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><Trophy className="size-6 text-secondary" /><div><p className="text-2xl font-semibold text-text">12 days</p><p className="text-xs text-text-muted">Best streak</p></div></CardContent></Card></div><Card><CardHeader><CardTitle>Subject progress</CardTitle></CardHeader><CardContent className="flex flex-col gap-5">{subjects.slice(0, 3).map((subject) => <ProgressBar key={subject.name} label={subject.name} value={subject.mastery} showValue variant={subject.mastery >= 80 ? 'success' : 'primary'} />)}</CardContent></Card><Card><CardHeader><CardTitle>Weekly progress</CardTitle></CardHeader><CardContent><ProgressBar label="Completed tasks" value={completed} max={5} showValue variant="secondary" /><p className="mt-2 text-xs text-text-muted">{completed} of 5 assignments completed</p></CardContent></Card></div> }

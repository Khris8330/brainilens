import { ArrowRight, CheckCircle2 } from 'lucide-react'
import { Card, CardContent, Badge, Button } from '@/components/ui'
import { initialAssignments } from '@/data/mockData'
export function StudentAssignmentsPage() { return <div className="space-y-6"><div><h1 className="text-2xl font-semibold text-text">Assignments</h1><p className="mt-1 text-sm text-text-muted">Stay on top of your upcoming work.</p></div><div className="grid gap-4">{initialAssignments.map((assignment) => <Card key={assignment.id}><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><Badge variant={assignment.status === 'completed' ? 'success' : assignment.status === 'in-progress' ? 'primary' : 'warning'}>{assignment.status === 'in-progress' ? 'In Progress' : assignment.status === 'completed' ? 'Completed' : 'Not Started'}</Badge><span className="text-xs text-text-muted">{assignment.subject}</span></div><h2 className="mt-2 text-base font-semibold text-text">{assignment.title}</h2><p className="mt-1 text-sm text-text-muted">{assignment.description}</p><p className="mt-2 text-xs text-text-muted">Due {assignment.dueDate}</p></div>{assignment.status === 'completed' ? <div className="flex items-center gap-2 text-sm text-secondary"><CheckCircle2 className="size-4" />{assignment.score}%</div> : <Button size="sm">{assignment.status === 'in-progress' ? 'Continue' : 'Start'}<ArrowRight className="size-4" /></Button>}</CardContent></Card>)}</div></div> }
